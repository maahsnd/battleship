import { createBoard } from './ui.js';

describe('grid creation tests', () => {
  test('create cell', () => {
    expect(createBoard().createCell(9)).toBe('<div class="cell"></div>')
  })
  test('create row', () => {
    
  })
  test('create grid', () => {
    
  })
})